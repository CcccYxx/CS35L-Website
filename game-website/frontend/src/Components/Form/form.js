import React from 'react';
import './Form.css';

const Form = () => {
    return(
        <div className="formContainer">
            <h2>Make a Post</h2>
        </div>
    );
}

export default Form;